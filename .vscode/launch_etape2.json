{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "WikiScan - Étape 2",
      "type": "python",
      "request": "launch",
      "program": "${workspaceFolder}/src/main.py",
      "cwd": "${workspaceFolder}",
      "args": ["--runId", "JD01", "--step", "2"],
      "console": "integratedTerminal",
      "env": {
        "PYTHONPATH": "${workspaceFolder}"
      }
    }
  ]
}
